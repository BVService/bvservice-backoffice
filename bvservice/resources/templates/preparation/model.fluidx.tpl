<?xml version="1.0" standalone="yes"?>
<openfluid>
  <model>
    <simulator ID="landprocessor.preparation-stage" enabled="1">
      <param name="EPSG_code" value="${EPSG_code}" />
      <param name="min_ent_size" value="${min_ent_size}" />
      <param name="landuse_field" value="${landuse_field}" />
    </simulator>
  </model>
</openfluid>
